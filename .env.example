# Required for live GitHub Releases during next build (local, CI, and Cloudflare).
# Classic PAT: public_repo
# Fine-grained PAT: Contents: Read on rustfs/rustfs, rustfs/console, rustfs/cli
# Cloudflare: Workers Builds / Pages → Settings → Variables/Secrets (repo code cannot set this).
GH_TOKEN=your_github_token
CDN_URL=https://cdn.example.com
